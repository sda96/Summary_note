퍼블릭에이아이 기술공장

통계학습 코스 1: ISLP
따라치면서도 자연스럽게 학습이 가능한 방법
통계학습 기반 인공지능 이해하기


code kata 연습과 반복으ㄹ통해 기술 연마 (기술 습득 방법)
코드 위주 - 
데코레이트 토픽
 1시간 분량 강의 4주치


코스
토픽 - 매주마다 1토픽씩
섹션 - 현지쌤 ppt 기준(추가내용 ppt)

이번주 10/27시작 
1주동안 intro 작성하기
intro에서는 전 챕터를 다룬다


github 인터페이스 git은 버전관리 시스템
파일명에 버전을 적으면 안된다 자체적으로 버전을 관리 해주기 때문

git date 
git log
git checkout 시점

git config --help
git config --global --help
git config --global user.name "sy"

git status ( 파일이 수정된 것을 보여준다)

git add(staging 수정된 파일 모아줌)
git add README.md 

git commit -m "다시 추가
1. ㄱㄷㅁ을ㄷㅂㄹ
2.ㄼㄹㄷㅂㄼ "
git log            origin은 github의 repo임
git push origin(별칭) master(브랜치)
push를 하기전까지는 local만 영향

1. git clone
2. 목차별 gitkeep 추가
01 tab

다음주 화요일까지 introduction
코스를 2개로 나눠도 됨 상/하 이후 챕터 5개 정도
 
- [x] 저거하기 체크만들기

closed #2
fixed #2

gitignore.io   ---------- gitignore
gitkeep이 비어있는 폴더


git pull gi

git add new.txt
git status
git commit -m "add
git.date
git push origin master
git rm -r 폴더명 <- github repo에 있는걸 제거해줌ㅎ
las -lah
git -ssh

git pull origin master
git pull
git log


intro는 좀 더 쉽게 
ppt 줄이고 글 줄이고 쉽게 


git pull origin master
git pull
git log
git push origin master
git log

##############
git status
변경 파일로 이동 위치 이동
git status
git add *.pptx <---- 수정파일은 제거
git add 0411_intro.pptx
git status
git commit -m "add 0411_intro"
git status
git log
git pull
git status
git push origin master

git pull origin 브랜치명 : 업데이트된 깃허브 내용을 로컬과 merge 시켜줍니다.
##############
git reset --hard HEAD https://www.google.com/search?q=git+reset+hard+&tbm=isch&ved=2ahUKEwiawZLQgPfsAhUXDJQKHcrpDq8Q2-cCegQIABAA&oq=git+reset+hard+&gs_lcp=CgNpbWcQAzIECAAQEzIECAAQEzIECAAQEzIECAAQEzIGCAAQHhATUNkXWItDYKtGaANwAHgAgAH2AYgBsguSAQUwLjcuMpgBAKABAaoBC2d3cy13aXotaW1nwAEB&sclient=img&ei=8gSqX5qAPJeY0ATK07v4Cg&bih=937&biw=1920&rlz=1C1CAFC_enKR922KR922#imgrc=Ii3nJ2GkcdXUeM
git reset --hard HEAD

git rm -r --cached 파일명
git date
git diff  <- 


git branch 브랜치명  <- 브랜치명 생성
git checkout 브랜치명 <- 브랜치 변경
(원하는 경로로 이동)
git add .
git commit -m "쏼라쏼라"
git push origin 브랜치명 <- 해당 브랜치에 push하면 해당 브랜치에 파일이 올라옵니다.

git rm -rf 폴더명 <- 폴더명에 있는 모든 파일을 제거합니다 (local과 github포함)


git clone -b {branch_name} --single-branch {저장소 URL} <- 특정 브랜치 clone
git remote add origin (저장소 url) <- github repo 연결

git fetch origin <- 원격 저장소로부터 가져온 모든 브랜치 헤드를 파일에 기록
https://mylko72.gitbooks.io/git/content/remote/remote_sync.html

git remote -v
git remote show
git remote rm 이름

git reset HEAD <- git add 내용 취소

git log를 나가는 방법은 q를 누르면 됨
git push, pull 을 사용하면 merge 편집기는 shift zz를 누르면 나가짐, :wq 를 사용하면 저장하고 편집기를 나감

(https://gmlwjd9405.github.io/2018/05/25/git-add-cancle.html) git add, git commit, git push 내용 취소


Conda 가상환경
conda info —envs
conda create —name 가상환경명
python -m ipykernel install --user --name tensorflow2.2.0 --display-name tensorflow2.2.0<- 커널 추가

jupyter kernelspec uninstall 가상환경이름 <- 커널 삭제
conda remove —name 가상환경명 —all <- 가상환경 삭제

conda install “package” <- 가상환경 패키지 설치
conda install package-name=2.3.4 -n some-environment <- 특정 가상환경에 특정 패키지 설치




remote: Repository not found 에러 발생, 단 privacy repo에 push 하는 경우 collaborator 추가
참고 사이트 --- https://o-yeon.tistory.com/90
초대 보내면 초대한 아이디의 등록 이메일주소로 초대장 보냄



